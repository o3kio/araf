# Upstream O3K gaps

This file is intentionally empty at planning time.

Implementation phases must record required O3K capabilities that are not confirmed by the authoritative upstream API instead of inventing console-only production semantics.

Use this format:

```text
## GAP-<n>: <short title>
Araf phase: Mx
User capability:
Missing authoritative O3K contract:
Why a frontend/BFF workaround would be unsafe or incorrect:
Proposed upstream direction (not yet approved):
Status: blocking | non-blocking | resolved
Upstream issue/PR:
```

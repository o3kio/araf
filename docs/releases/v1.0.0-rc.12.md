# Araf v1.0.0-rc.12 release notes

First versioned PP demo release bundle. The OCI images were built, scanned
(Trivy HIGH/CRITICAL gate) and attested (buildkit SBOM + SLSA provenance +
GitHub OIDC build provenance) by `.github/workflows/release-images.yml`; the
GitHub Release assets were assembled by `.github/workflows/release-publish.yml`
from those attested images.

## Source

- Tag: `v1.0.0-rc.12` (lightweight, points at the commit below)
- Source SHA: `de64cc9193085116fa30ad51c04ccab24a013dd0`

## Release images

Pull and deploy by digest only; GHCR tags are mutable and are not repeated
here as authority.

| Component | Image |
| --- | --- |
| bff | `ghcr.io/o3kio/araf-bff:v1.0.0-rc.12@sha256:bc717ecdbbbf3ea673efe168c90419936677d644aa0ae25af4eb84906cd744ba` |
| tenant-console | `ghcr.io/o3kio/araf-tenant-console:v1.0.0-rc.12@sha256:25f5fe41927f68db3dafd49597c6b8cb4520bca2dd45ec131d1372474ef3e5e5` |
| operator-console | `ghcr.io/o3kio/araf-operator-console:v1.0.0-rc.12@sha256:cbbad76033eced4d4290c9848e0665a23c30bd4a7c647077ba0cf03150666b18` |

## Release assets

Published on the GitHub Release for `v1.0.0-rc.12`:

- `araf-v1.0.0-rc.12-deploy.tar.gz` — reference Compose deployment
  (`docker-compose.release.yml`, unmodified), `ENVIRONMENT.md`,
  `digests.txt`, `VERIFY.md`.
- `araf-v1.0.0-rc.12-digests.txt` — image digest pins.
- `bff-v1.0.0-rc.12.spdx.json`, `tenant-console-v1.0.0-rc.12.spdx.json`,
  `operator-console-v1.0.0-rc.12.spdx.json` — SPDX SBOMs extracted from the
  GHCR image attestation manifests; checksum manifest
  `araf-v1.0.0-rc.12-sbom.sha256`.
- `bff-v1.0.0-rc.12.provenance.json`, `tenant-console-v1.0.0-rc.12.provenance.json`,
  `operator-console-v1.0.0-rc.12.provenance.json` — SLSA v1 provenance
  extracted from the same attestation manifests; checksum manifest
  `araf-v1.0.0-rc.12-provenance.sha256`.

Verify checksums with `sha256sum -c` against the downloaded assets and
re-resolve image digests as described in `VERIFY.md` (inside the deploy
tarball) and [docs/operator/installation.md](../operator/installation.md).

## Known limitations

- **GHCR tags are not platform-immutable.** The published digests are the
  cryptographic/policy authority and the attestation subjects, but the
  registry does not enforce tag immutability; always pin and verify by
  `@sha256:` digest.
- **Keycloak / external IdP is not part of the Araf bundle.** Araf requires
  a supported external OIDC provider with one confidential client per
  surface; identity-provider deployment, TLS certificates and DNS remain
  deployment-boundary concerns.
- **1h upstream token TTL is an accepted deviation.** The O3K/OIDC upstream
  token lifetime of one hour is accepted for this demo phase; longer-running
  operator sessions re-authenticate on expiry. BFF session custody and
  rotation behavior are unchanged.

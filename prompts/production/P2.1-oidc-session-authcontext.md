# P2.1 — Real O3K OIDC, BFF session and AuthContext convergence

## Goal
Close the remaining Araf-side production human-authentication work using the completed O3K P12-IAM contract while preserving separate Tenant and Operator trust surfaces.

## Authoritative baseline

Do not treat O3K federation as missing.

P12-IAM is complete:

- O3K merge: `e47a81861f1f2c5dac125d017366a959dca39d1b`;
- Araf integration merge: `01b234ba228b0dccfbbb794819933f5d48f97a31`;
- Araf aggregate evidence: `fd790f5ba9a6841d8a14be7b89e3d302a3135182`;
- real Keycloak -> Araf Tenant BFF -> O3K scope discovery/exchange/resource/logout journey: PASS;
- O3K P12-IAM aggregate verdict: PASS;
- Araf production identity unblocked: YES.

O3K now authoritatively provides federated scope discovery, federated-to-native scoped token exchange, canonical `/identity/me` AuthContext and explicit system/operator authorization.

Do not reopen or redesign O3K IAM unless a reproducible contract regression is found.

## Closure status

P2.1 is closed in Araf. The implementation and required CI gates are merged on
`main`. The remaining end-to-end production acceptance is intentionally owned
by P2.8, which must rerun the complete real Tenant and Operator journeys in a
secured deployment environment.

## Historical Araf gaps closed by P2.1

1. **Provider-neutral OIDC client behavior** is implemented with standards-based discovery metadata, issuer consistency checks, bounded reads, endpoint validation and controlled redirects.
2. **Production configuration** fails closed; missing or malformed configuration cannot select fixtures or localhost defaults.
3. **Operator BFF convergence** is implemented with a separate OIDC client/session and O3K system-scope authorization path. The full secured deployment rerun is a P2.8 responsibility.
4. **Canonical context projection** uses O3K native identity/scope truth; IdP profile fields are display-only.
5. **Session lifecycle** preserves logout, expiry, fixation, CSRF, state and S256 PKCE protections. Multi-replica/durable session storage remains P4.3.

## Implement/verify
- OIDC Authorization Code flow through confidential BFF clients with RFC 7636 S256 PKCE;
- distinct Tenant and Operator OIDC/session configuration and cookie namespaces;
- standards-based OIDC discovery and endpoint use, without Keycloak-specific production URL derivation;
- secure server-side IdP/O3K token custody;
- authoritative O3K scope discovery and native token exchange;
- current-user/AuthContext projection derived from O3K;
- real Operator BFF system-scope journey;
- logout and expiry behavior;
- negative tests: tenant session to operator route, expired session, invalid/replayed state, CSRF, manipulated scope/project, wrong issuer/audience, missing binding and unauthorized operator access;
- no access/refresh/native token in browser-readable persistent storage.

## Acceptance
P2.1 is complete only when:

- a real tenant authenticates through a standards-based IdP and receives authoritative O3K project context;
- a separately configured real operator authenticates through the Operator BFF and receives only explicitly authorized O3K system context;
- tenant -> operator and cross-project attempts are denied server-side by O3K;
- provider endpoint discovery is standards-based rather than Keycloak-path-specific;
- production configuration cannot silently fall back to fixtures;
- browser inspection cannot recover reusable IdP/O3K credentials;
- all Araf frontend/Rust/browser gates pass.

Do not block P2.1 on P4.3 HA/session-sharing work unless the supported deployment profile already claims multiple BFF replicas.
